# Page not found (HTTP 404)

There is no page at the requested address on `projects.dev`. This response is a real
`404` — the site does not return `200` for unknown paths, so a path that 404s here
does not exist.

## Where to look next

- `/` — Home: what Stripe Projects is and how to install it
- `/providers/` — Provider catalog: every supported provider and service slug
- `/templates/` — Templates with providers pre-wired
- `/developers/` — Developer resources index (CLI, agent skill, API discovery)
- `/blog/` — Changes and announcements

## Machine-readable resources

- `/llms.txt` — Curated overview of this site (llmstxt.org)
- `/sitemap.xml` — Every canonical URL on the site
- `/skill.md` — Agent skill for end-to-end provisioning
- `/index.html.md` — Full markdown rendering of the home page
- `/.well-known/api-catalog` — RFC 9727 linkset for API discovery
- `/.well-known/agent-skills/index.json` — Agent skill discovery document
- `/api/openapi.json` — OpenAPI 3.1 description of the public endpoints
- `/api/v1/health` — JSON health check
- `/auth.md` — Authentication status for this site
- `/robots.txt` — Crawler access rules

## Requesting this as markdown

A request to any nonexistent path with `Accept: text/markdown` returns a `404` whose
body is markdown rather than HTML. Unrecognised paths under `/api/` return a `404` with
an RFC 9457 `application/problem+json` body instead.

## Product documentation

The public site is a small static surface. The product itself is a Stripe CLI plugin,
and its documentation lives on Stripe's docs site:

- Install: https://docs.stripe.com/stripe-cli/install
- Stripe Projects docs: https://docs.stripe.com/projects
- Stripe API reference: https://docs.stripe.com/api
